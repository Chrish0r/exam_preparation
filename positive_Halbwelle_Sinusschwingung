#include <stdio.h>
#include <math.h>



int main() {

    int zeile;
    int spalte;

    for(zeile = 0; zeile < 25; zeile++) {
        for(spalte = 0; spalte < sin(zeile / (25 / M_PI)) * 40; spalte++) {
            printf("*");
        }
        printf("\n");
    }

    return 0;
}
